[package]
name = "prono_api"
version = "0.1.0"
edition = "2024"

[dependencies]
mockall = { version = "0.14" }
serde = { version = "1.0", features = ["derive"], optional = true }

[features]
test-utils = []
serde = ["dep:serde"]
