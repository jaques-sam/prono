[package]
name = "prono-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
prono = { path = "../prono" }
prono_db = { path = "../prono_db" }
clap = { version = "4", features = ["derive"] }
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
env_logger = "0.11"
