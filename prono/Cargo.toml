[package]
name = "prono"
version = "0.1.0"
edition = "2021"

[dependencies]
chrono = "0.4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
uuid = { version = "1.11.0", features = ["js", "v4", "v5"] }

[dev-dependencies]
rstest = "0.23.0"
